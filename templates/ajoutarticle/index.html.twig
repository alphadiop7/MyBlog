{% extends 'base.html.twig' %}

{% block title %} {{ title }} {% endblock %}

{% block body %}
<div class="container-fluid mt-2">
    <div class="row">
        <div class="col-lg-8 mx-auto shadow p-4 rounded">
            <div class="card">
                <div class="card-header">
                    <h3>Ajout d'un nouvel article au blog</h3>
                </div>
                <div class="card-body">
                    {{ form(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
